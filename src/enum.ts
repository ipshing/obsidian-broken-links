export enum LinkGrouping {
    ByFolder,
    ByFile,
    ByLink,
}

export enum FolderSort {
    NameAsc,
    NameDesc,
}

export enum FileSort {
    NameAsc,
    NameDesc,
    CountAsc,
    CountDesc,
}

export enum LinkSort {
    NameAsc,
    NameDesc,
    CountAsc,
    CountDesc,
}
